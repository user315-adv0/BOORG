{
  "name": "bookmark-meta-scanner",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rimraf dist && mkdir -p dist && esbuild src/background.ts --bundle --outfile=dist/background.js --platform=browser --format=esm && esbuild src/popup.ts --bundle --outfile=dist/popup.js --platform=browser --format=iife && esbuild src/export.ts --bundle --outfile=dist/export.js --platform=browser --format=iife && cp src/popup.html dist/popup.html && cp src/popup.css dist/popup.css && cp src/export.html dist/export.html && cp manifest.json dist/manifest.json",
    "dev": "npm run build -- --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.274",
    "esbuild": "^0.23.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}

